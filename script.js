const myArr=[
  5, 9, 37, 22, 49, 3
]


function isInArray(myArr) {
    //for (let i = 0; i = myArr.lenght; i++) { // checking each item
    for (myArr.includes(el)) {
      result = true;
      console.log(result);
    }   
    return result;
  }